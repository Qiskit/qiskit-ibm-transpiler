Fixed GitHub Actions CD workflow error by replacing the invalid ``python-version`` parameter in the ``astral-sh/setup-uv@v3`` action with a separate step using ``uv python install 3.11`` to properly install Python 3.11.
